const email = 'Email';

const globalHeader = {
  availabilityCalendarPage: 'AvailabilityCalendar__c',
  myCalendarPage: 'MyCalendar__c',
  patientsPage: 'Patients__c',
  resourcesPage: 'Resources__c',
  servicesPage: 'Services__c',
};

const modalSize = {
  LARGE_SIZE: 'large',
  MEDIUM_SIZE: 'medium',
  SMALL_SIZE: 'small',
};

const roles = {
  physician: 'Physician',
  cartCoordinator: 'CAR-T Coordinator',
};

export {
  email,
  globalHeader,
  modalSize,
  roles,
};
